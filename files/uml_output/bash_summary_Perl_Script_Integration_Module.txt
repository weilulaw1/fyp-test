@startuml
class Perl_Script_Integration_Module {
  Summary:
  --
  This module provides functionality to
  embed and execute Perl scripts within
  shell or C environments, enabling seamless
  script integration and execution.
  --
  name: Perl Script Integration Module
}
class Perl_Integration_and_Embedding_Module {
  Summary:
  --
  This module enables embedding and
  This module provides functionalities to
  embed and execute Perl scripts within a
  executing Perl scripts within shell or C
  environments to facilitate script
  shell or C environment, facilitating
  integration and execution.
  script execution and integration.
  --
  name: Perl Integration and Embedding Module
}
class bperl {
  Summary:
  --
  The file implements a shell builtin
  command that interfaces with a Perl5
  interpreter, allowing execution of Perl
  scripts within the shell environment.
  --
  bperl_builtin()
  summary: string
}
class iperl {
  Summary:
  --
  The code initializes, runs, and cleans up
  a Perl interpreter within a C program,
  enabling execution of Perl scripts from C.
  --
  perl_main()
  summary: string
}
bash --> Perl_Script_Integration_Module
Perl_Script_Integration_Module --> Perl_Integration_and_Embedding_Module
Perl_Integration_and_Embedding_Module --> Perl_Integration_and_Embedding_Module
Perl_Integration_and_Embedding_Module --> bperl
Perl_Integration_and_Embedding_Module --> iperl
@enduml